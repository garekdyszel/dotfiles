# -*- sh -*-
#!/bin/bash

GIT='/usr/bin/git'
HOME_REPO='/home/chips/'
cd $HOME_REPO

# add all changed files
$GIT add --all

# add all submodules (research, classes, etc.)
# for x in $(find . -type d) ; do\
#     if [ -d "${x}/.git" ] ; then\
#         cd "${x}"\
#         origin="$(git config --get remote.origin.url)"\
#         cd - 1>/dev/null\
#         git submodule add "${origin}" "${x}"\
#     fi\
# done

$GIT commit -m "$(date "+%Y_%m_%d") routine backup"
$GIT push origin master
